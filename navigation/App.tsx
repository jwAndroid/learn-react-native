/**
 * @format
 */

import { View, Text } from 'react-native';
import React, { memo } from 'react';

function App() {
  const a = '';

  return (
    <View>
      <Text>App</Text>
    </View>
  );
}

export default memo(App);
