import { View, Text } from 'react-native';
import React, { memo } from 'react';

function TwoScreen() {
  return (
    <View>
      <Text>TwoScreen</Text>
    </View>
  );
}

export default memo(TwoScreen);
