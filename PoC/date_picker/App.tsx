/**
 * @format
 */

import {View, Text} from 'react-native';
import React, {memo} from 'react';

const App = () => {
  return (
    <View>
      <Text>App</Text>
    </View>
  );
};

export default memo(App);
